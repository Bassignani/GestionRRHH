{
  "name": "smart-recruitment-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo para Smart Recruitment (Client/Server)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
